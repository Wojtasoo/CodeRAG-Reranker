# LLM provider options: "local", "openai", "custom"
LLM_PROVIDER = "local"

# Embedding provider options (currently "sentence_transformers" supported via SentenceTransformer or "openai" via OpenAI API)
EMBEDDING_PROVIDER = "sentence_transformers"

# Default embedding model for SentenceTransformer
EMBEDDING_MODEL = "all-MiniLM-L6-v2"
